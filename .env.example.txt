# Azure OpenAI 설정
# Azure Portal에서 OpenAI 리소스를 생성한 후 다음 정보를 입력하세요.

AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Azure Embedding 설정
# Embedding 모델 배포 이름을 입력하세요.
AZURE_EMBEDDING_DEPLOYMENT=text-embedding-ada-002

# LangSmith 설정 (선택사항)
# LangSmith를 사용하여 에이전트 실행을 추적하고 디버깅할 수 있습니다.
# https://smith.langchain.com/ 에서 API 키를 발급받으세요.

LANGSMITH_API_KEY=your-langsmith-api-key-here
LANGSMITH_PROJECT=patent-analysis

# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일을 생성하세요:
#    Windows: copy .env.example .env
#    Linux/Mac: cp .env.example .env
#
# 2. .env 파일을 열어서 실제 값으로 수정하세요.
#
# 3. .env 파일은 절대 Git에 커밋하지 마세요! (.gitignore에 포함되어 있음)
